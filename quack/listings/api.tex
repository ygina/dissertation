\begin{lstfloat}[t]
\begin{lstlisting}[language=Rust]
trait quACK {
    fn count() -> usize;
    fn last_element() -> u32;
    fn encode(element: u32);
    fn remove(element: u32);
    fn sub(rhs: quACK) -> quACK;
    fn decode() -> &[u32];
}
\end{lstlisting}
\captionof{lstlisting}{Pseudocode interface for the quACK implementations in
 the software library. Each element is a 4-byte random identifier
 referring to a network packet.}
\label{lst:quack:api}
\end{lstfloat}
