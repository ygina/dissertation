\begin{lstfloat}[t]
\begin{lstlisting}[language=Rust]
trait quACK {
    fn count() -> usize;
    fn last_element() -> u32;
    fn encode(element: u32);
    fn remove(element: u32);
    fn sub(rhs: quACK) -> quACK;
    fn decode() -> &[u32];
}
\end{lstlisting}
\captionof{lstlisting}{Pseudocode interface for the implementation of a quACK
 construction in our software library~\cite{quack-github}. An element is an
 unsigned 32-bit integer referring to a 4-byte packet identifier.}
\label{lst:quack:api}
\end{lstfloat}
